# Claude Code Journal

This journal tracks substantive work on documents, diagrams, and documentation content.

---

1. **Task - Project initialization and scaffold prune** (v0.1.0): Initialised Claude configuration and reshaped the copier-data-science scaffold into a two-package tooling repository<br>
   **Result**: The repository was generated from `copier-data-science` v1.3.18 but the project is a terminal tool, not a data science project, so the data science scaffolding was pruned: removed `data/`, `models/`, `notebooks/`, `reports/`, `references/` and the `dataset.py`, `features.py`, `plots.py` and `modeling/` modules. Created `src/copier_ui/` alongside `src/copier_tui/` and updated `pyproject.toml` to package both, extend ruff `src`/`include` to all of `src`, list both as isort first-party, and swap the data dependencies for `copier`, `textual` and `rich`. Trimmed `config.py` to logger, dotenv and `PROJ_ROOT`, dropped the now-orphaned `make data` target and the `/data/**` gitignore block, and wrote `.claude/CLAUDE.md` carrying the reinforced bans, project context and strengthened layering rules.

2. **Task - Design capture and README rewrite** (v0.1.0): Extracted the shared design conversation and rewrote the README around the two-package architecture<br>
   **Result**: The design lived in a ChatGPT share link whose page is a client-side SPA, so `WebFetch` returned only the title; fetched `https://chatgpt.com/backend-api/share/<id>` directly instead and reconstructed the seven-message thread from the `mapping` JSON. The design: copier extensions are Jinja extensions, not pluggable questionnaire renderers, so copier stays the template engine and answers are supplied through `copier.run_copy(..., data=answers)`, which suppresses interactive prompting. `copier-ui` owns semantics across three layers - copier adapter, UI model, state engine - and must never require a terminal, browser or event loop; each `copier-*ui` owns presentation only. Rewrote `README.md` with that split, the kind-to-widget mapping table, an ASCII architecture diagram, the `TemplateUI` API sketch, and a usage section stating the CLI drop-in property.

3. **Task - Acceptance criteria for both packages** (v0.1.0): Wrote the consolidated acceptance criteria document covering `copier-ui` and `copier-tui`<br>
   **Result**: Created `docs/acc-crit-copier-tui.md` as a single gate with a Contents TOC and one section per package, following the `acceptance-criteria` skill. The `copier-ui` section fixes purity (no display library, no event loop, synchronous API), question-kind normalisation, choice ordering, dependency graph, visibility recomputation, computed defaults, explicit-versus-default tracking, secret exclusion from dumps, determinism, and a `### API` block for the `TemplateUI` surface. The `copier-tui` section fixes the CLI drop-in requirement - copier's exact subcommands, arguments, flags and short forms, with `--data` seeding and `--defaults`/`--quiet` staying headless - plus widget mapping, free back-and-forth navigation, live revisibility, the review screen and the rule that nothing is written before confirmation. Both sections enumerate edge cases. `acc-crit.py check` reports clean.

4. **Task [Extended] - Monorepo split, GitHub repo and build workflow launch** (v0.6.1): Split into two publishable packages, created the public GitHub repo, wired CI, and launched the multi-agent build<br>
    **Result**: Two PyPI packages need two build configs, so `src/` became a uv workspace: `packages/copier-ui/` and `packages/copier-tui/`, each with its own `pyproject.toml`, `README.md` and PyPI metadata, and a virtual workspace root carrying ruff, pytest and the dev group. Version set to 0.6.1 in lockstep; `scripts/bump_version.py` bumps the shared patch number and moves the exact `copier-ui==` pin inside copier-tui, and `make install` runs it every time - a major change bumps the patch too, deliberately. `make publish` uploads both distributions with twine, copier-ui first because copier-tui pins it exactly. `make test-functional` builds both wheels, creates a throwaway venv under `tmp/functional-venv`, installs the wheels and runs `tests/functional` against them, which is what the CI functional job repeats. First install failed on PEP 639: setuptools rejects the deprecated `License :: OSI Approved :: MIT License` classifier alongside the SPDX `license = "MIT"` expression - dropped the classifiers. Created `stellarshenson/copier-tui` public on GitHub via the REST API (no `gh` CLI in this environment) and pushed. Added `.github/workflows/build.yml`: ruff, unit tests on Python 3.11 through 3.13, then wheels, functional tests and `twine check`. Launched background workflow `wf_f336133b-cd8` (Opus executors, contract-first: architect fixes module signatures, then copier-ui and copier-tui implemented in parallel, then three test suites). Resume after a container restart with `Workflow({scriptPath: ".claude/projects/-home-lab-workspace-private-copier-projects-copier-tui/3ec7341b-62a6-4df4-ba33-1be41c866e9f/workflows/scripts/copier-tui-build-wf_f336133b-cd8.js", resumeFromRunId: "wf_f336133b-cd8"})` under `~/.claude/`; finished agents return cached results in seconds.

5. **Task [Extended] - copier-ui and copier-tui implemented** (v0.6.1): Contract-first build landed both packages; the test phase was killed by a session limit and relaunched<br>
    **Result**: Workflow `wf_f336133b-cd8` completed 2 of 6 agents before the session limit hit - architect and `copier_ui` implementer finished, the `copier_tui` implementer and all three test agents died, though `copier_tui` files had already been written to disk. The architect's `docs/design-notes.md` maps which copier 9.17.2 surfaces are stable (`run_copy`, `run_recopy`, `run_update`, `Phase`, `Settings`, `VcsRef`, `load_settings`) versus internal, and pins the rule that internals come from `copier._main` / `copier._user_data` / `copier._template`, never the `copier.main` shims which warn per attribute access. Three forced deviations from the morning design: `from_template` now takes destination and operation because copier-data-science's first default is `{{ _copier_conf.dst_path.name }}`, so a template cannot be loaded without knowing where it will be written; the engine receives `Evaluator` / `FieldValidator` callables instead of importing the adapter, keeping the dependency graph one-directional and the engine testable without a template; `copier_tui` gained `theme.py` so screens do not have to import `app.py` for palette constants. `copier_ui` is 722 lines across five modules, `copier_tui` 1041 across cli, app, three screens, widgets, theme and screenshots. The adapter strips a question's own `validator` when evaluating defaults, otherwise a template default that fails its own validator would disable the field instead of surfacing through `validate()`. Discovered that copier-data-science eagerly loads `jinja2_time.TimeExtension`, so `jinja2-time` had to join the dev group and must reach the functional throwaway venv. Verified by hand: `copier-tui --help` prints copier's own help with its subcommands, and `copier-tui copy --defaults --trust` renders the reference template into a working project, exit 0. Pushed as `ee79da7`. Relaunched the remaining work as workflow `wf_f1aefe72-417` (audit copier_tui, three test suites in parallel, then an integration pass to green); resume with `Workflow({scriptPath: "<session>/workflows/scripts/copier-tui-tests-wf_f1aefe72-417.js", resumeFromRunId: "wf_f1aefe72-417"})`.

6. **Task [Extended] - Green CI, then three-lens adversarial review of the TUI** (v0.6.4): Drove lint, unit and functional green in CI, then reviewed the terminal UI with three adversaries and adjudicated 58 findings into one change plan<br>
    **Result**: 125 unit tests and 8 functional tests landed; the functional job failed only on the GitHub runner because the reference template's post-generation task shells out to a bare `python3` and imports `jinja2` - on the runner that resolved to the system interpreter, so every render wrote the whole tree then died on the last step, a failure that could not reproduce locally where the ambient `python3` happens to carry jinja2. Fixed by putting the throwaway venv's `bin` first on PATH in both the Makefile and the workflow, which is what an activated venv gives a real user. All five CI jobs green at `8b6e37d`. Also stopped `make clean` deleting `logs/`, which was destroying the log of the CI watch running at the time. The Star Colonel then called the TUI too sprawling, so `ux-designer`, `analyst` and `tui` lenses each reviewed it and each returned DO-NOT-SHIP - 58 findings. An adjudicator re-verified every one against the live tree: the survey spends 172 terminal rows on 23 questions (eight screenfuls at 80x24); escape on a dropdown discards all answers with no confirmation; enter cannot confirm on 17 of 23 fields; template code executes at load with no `--trust`, making us strictly less safe than copier itself; a secret's value prints in clear under its masked input; `--pretend` reports files it never wrote, and the shipped screenshot was that run. Four claims were refuted and three remedies declined, one because it seeds a crash - `allow_blank=False` raises `InvalidSelectValueError` on exactly the recompute path it was meant to help. Ruled 11 changes, 5 deferred. The lens disagreement about replacing the overview screen was resolved by deleting it outright, since the review screen already lists every answer, and fixing back-navigation by re-pushing one held `SurveyScreen` instance - three lines, proven to preserve scroll and focus. Process fault recorded: five of nine renderer files changed inside the review window because an integration agent was still running, which is why line numbers went stale. Freeze the tree, then review. Launched workflow `wf_6a509a8b-85e` to apply the plan; resume with `Workflow({scriptPath: "<session>/workflows/scripts/copier-tui-redesign-wf_6a509a8b-85e.js", resumeFromRunId: "wf_6a509a8b-85e"})`.

7. **Task [Extended] - Survey rebuilt as a one-line-per-question form** (v0.6.5): Applied the adjudicated redesign by hand after the workflow died mid-flight, closing 76 of 78 acceptance criteria and putting the three screens in the README<br>
    **Result**: Workflow `wf_6a509a8b-85e` landed only its Core phase - the `copier_ui` trust gate (`Worker._check_unsafe` before `jinja_env`, since that access imports every `_jinja_extensions` entry) and secret redaction in adapter error messages - before dying with the session; the remaining phases were done directly rather than relaunch a third time. `FieldRow` went from a `Vertical` of label, help, control and error line to a `Horizontal` of right-aligned label gutter, flat `compact=True` control and one status glyph, with help and validation messages moved to a single reserved hint line that follows the focus. Measured on copier-data-science: 24 questions in 24 rows against the old layout's 172 for 23. `JumpScreen` and the validation popup are gone - a blocked confirm focuses the first bad field and names it. The headline back-navigation requirement is met by stacking `ReviewScreen` over `SurveyScreen` rather than replacing it, so nothing is rebuilt: `SurveyScreen` became `Screen[None]` posting `Confirmed`/`Cancelled` for the app to act on, and a pilot test asserts scroll offset and focus survive the round trip, and that a later answer still recomputes. One key-ownership rule in `check_action`: enter reaches only an open menu and a multiline editor, so a switch and a closed select keep confirming - the adjudicator's list included `Switch`, dropped because space already toggles it; arrows hand focus on at a control's first and last line instead of trapping it; escape arms once and quits on a second press, the Star Colonel's choice over deleting the binding. Found and fixed a Textual trap worth remembering: `Select.__init__` stores its value in `self._value` and only assigns the reactive in `_on_mount`, so a row writing the same value in first left the reactive unchanged, the watcher never fired, and every choice control rendered `select...` over an answer it was holding - rows now leave unmounted controls alone. The execution verdict moved onto the status line it had been duplicating in an overlay, a `--pretend` run says nothing was written, and both `ProgressBar` and its inner `Bar` needed a width rule - auto and a fixed 32 cells respectively. `--quiet` left the headless predicate (copier's `-q` suppresses output and still asks) and `--ask` now overrides `--defaults`. 148 unit tests and 8 functional tests pass, CI green at `8a187cf`, README carries all three regenerated SVGs. Note: `make clean` removes `tmp/`, where graphify artefacts live, so `make build` destroys any graph.

8. **Task - First PyPI release of both packages** (v0.6.8): Published `copier-ui` and `copier-tui` to PyPI in lockstep<br>
    **Result**: Closing work after the redesign: escape is handed back to an expanded `Select` so an open dropdown closes instead of arming a quit - the screen's binding is `priority=True` and was pre-empting `SelectOverlay`'s own escape - and an armed cancel now lapses after 3s instead of staying armed for the session. The three README screenshots were regenerated from a real render into a throwaway directory rather than a `--pretend` run, so the execution capture reports files actually written. `logs/README.md` is tracked so a fresh clone explains the log directory. 149 unit tests, 8 functional tests against the built wheels, ruff clean, CI green at `723c409`. Adversarial review stands at one round on the renderer only - `copier_ui` and the rebuilt survey are unreviewed.

9. **Task - Captions replace variable names in the survey** (v0.6.9): Applied the four critical findings of a ux adversarial review<br>
    **Result**: The survey showed `pydata_packages` where copier's own prompt shows `Include basic PyData packages (numpy, pandas, scikit-learn, matplotlib)` - `adapter.py` hardcoded `label=id`, so no UI built on it could ever show a caption. It now takes copier's `Question.get_message()`, which renders the template's `help` and falls back to `var_name (type)`. Help was hidden twice over: `_show_hint` returned the constant `space opens the list` before reaching help, leaving all 18 choice fields unexplained; help now outranks the key hint. The caption gutter is a 45% share capped at 56, ellipsis-marked, full text on the hint line. Two correctness bugs went with it - an open dropdown had lost Textual's overlay border and painted bare values over the rows beneath, and picking a Select's own prompt row left `no answer yet` on screen while state kept the real value the render then used. 156 unit tests, 8 functional.

10. **Task - Question grouping without a copier concept** (v0.6.9): Answered how a UI could know categories when `copier.yml` declares none, and built the two routes that hold<br>
    **Result**: Several design prototypes drew headings like `Tooling and quality`, which the template never states. Read copier 9.17.2's `filter_config` (`copier/_template.py:61`): every `_`-prefixed key becomes `config_data` with the underscore stripped, and copier validates no key set, so an unrecognised one is carried through and ignored - proven by rendering a probe template declaring `_ui_groups`. Two routes shipped in `copier_ui`: `Schema.groups` reads that opt-in key, and a template naming none (nearly all, including copier-data-science) gets one untitled group covering every question, so a frontend has one code path; `Question.condition_ids` names what a `when` reads, nesting the eight conditional fields of copier-data-science automatically. Groups never reorder questions, and a malformed key loads as absent. Prefix and comment derivation were measured and rejected - 5 clusters over 11 of 31 fields, banners naming non-categories. Three fixtures, 169 tests, six criteria closed.

11. **Task [Extended] - Evidence base for the questionnaire design** (v0.6.9): Ran a 13-agent research workflow over questionnaire and settings-UI design, adjudicated its findings against the primary sources, and wrote the reference the candidate designs are judged against<br>
    **Result**: Workflow `wf_9a43fc71-ec9` produced `docs/research-settings-ui.md`, 6,398 words: a 20-row claim/verdict table, 53 imperative rules with sources and costs, prior art from menuconfig, nconfig, Kconfiglib, debconf, VisiData, btop and GOV.UK, 16 traps, and a closing section naming the four gaps nobody has measured. The point of the exercise was that the design conversation had been running on folklore. Ten of the twenty claims fail against their own primaries: choice overload (Scheibehenne 2010, D = 0.02, N = 5,036, and Chernev 2015's own no-moderator model agreeing at p = .48), Miller's 7 plus or minus 2 (his paper calls it "a pernicious, Pythagorean coincidence" and measures absolute judgment, not visible lists), Hick's law (Liu, CHI 2020: "Hick's law speaks against, not for, the popular principle that 'less is better'"), and progress indicators (meta-analysis null, 1.56x dropoff odds for the slow-then-fast shape this survey produces). Decisively for the work in flight, no study in the entire choice-overload literature ever manipulated visibility - every arm showed every option - so hiding alternatives behind a dropdown has no evidential support at all, and named sections have a direct null (Smyth 2006 isolated headings from spacing: 64.6% against 66.8%, p = .538) while the spacing carries the effect. The finding that reverses an earlier recommendation is Luguri and Strahilevitz: presenting the same content less prominently doubled acceptance from 14.8% to 30.1%, so dimming untouched defaults is hiding them, not styling them, and the cheap-verification result (Eggs and Jackle: task difficulty predicts confirming a false preload, motivation does not) says the untouched rows are exactly the ones that must stay legible. The evidence therefore favours a dense, always-visible, undimmed list over both the accordion and the two-tier brightness encoding. `docs/README.md` indexes the file.

12. **Task [Extended] - Clarity search judged, then measured** (v0.6.9): Sixteen designs searched one axis, a bracket named a winner, and a mechanical contrast audit re-checked the result<br>
    **Result**: Workflow `wf_1cf204e4-974` produced sixteen designs, four semifinals of four, and a final. The winner is `Bare accordion` - the incumbent accordion stripped to a two-column list, one soft plate and one hue, with the five-row option stack collapsed to a single line. Its winning argument was the three-step brightness ramp, which is exactly the mechanism `docs/research-settings-ui.md` had just disqualified, so the judge's verdict was re-checked with `scratchpad/build/audit.py`: WCAG contrast per row, resolved against the ground each row actually sits on rather than the screen. Seven of sixteen paint text at 3.25:1, under the 4.5:1 floor, and in every case it is dim ink on the lifted focus plate - the alternatives of the question being decided are the least legible thing on screen. The judge's runner-up is one of the seven. The winner is not: it clears at 4.54:1 and puts only 14 percent of its ink at the dim tier. One defect survives both passes - its twenty-one untouched defaults sit at 4.54:1 while the two answers already given sit at 6.15:1, so the rows needing verification are dimmer than the rows already settled, which is the inversion Luguri and Strahilevitz measured. Fix is a tier swap, not a redesign. Specimen book rebuilt to 40 plates with the measured verdicts inline.

13. **Task [Extended] - Survey rebuilt as the bare accordion** (v0.6.9): Replaced the dropdown form with options printed on the row, wrapped captions and per-question help<br>
    **Result**: Rendering the live app rather than trusting the design plates confirmed every complaint and found worse: captions were right-aligned so each began at a different column, twelve of twenty-three were cut with an ellipsis, and an open `Select` painted its overlay across six other questions with a broken border. New `inline.py` holds `InlineOptions`, a focusable one-line widget printing every option with the one in force lit; left and right walk it, space cycles, and nothing ever opens. `ChoiceSelect`, `ChoiceSelectionList` and `Switch` are gone with it, along with the six tests guarding defects the structure no longer has. `FieldRow` became a `Vertical`: a caption-and-control line, then a help line the focused row reveals. Captions left-align in a 56-column gutter and wrap - 56 measured as the widest gutter at which every choice in the reference template still fits its options on the same row. `WrapInput`, a `TextArea` that gives enter back to the screen, replaces `Input` for string and path so a long description wraps over three lines instead of scrolling out of a one-line box, with the cursor placed at the end on mount or the first backspace would do nothing. Placeholders moved out of the value column into the help line as `for example: ...`, since a placeholder rendered where an answer goes reads as an answer. Untouched defaults are no longer dimmed or flagged - per Luguri and Strahilevitz de-emphasis is concealment, and the untouched rows are the ones needing checking. Discovered while fixing tests that copier has no description separate from the caption: `get_message()` returns the rendered `help`, so the two are the same string and the user's missing help was the truncation all along. 169 tests, ruff clean, README and its three captures regenerated.

14. **Task - Review screen wraps too, and the criteria catch up** (v0.6.9): Carried the no-truncation rule onto the confirmation screen and rewrote the criteria the rebuild invalidated<br>
    **Result**: `review.py` still held `_gutter`, cutting seven of twenty-four captions with an ellipsis on the last screen before anything is written - the worst place for it, since those are the words someone reads to decide whether to go ahead. Replaced the single padded `Static` per answer with a `Horizontal` of a wrapping caption and a wrapping value, matching the form, and added a test asserting no ellipsis reaches the screen at all. Seven acceptance criteria described mechanisms that no longer exist - the dropdown's bordered overlay, the shared hint line, the help-outranks-key-hint rule - and were reworded against what replaced them rather than left to rot; six new ones record what the rebuild guarantees. 91 of 92 criteria closed, 170 unit tests, 8 functional tests against the built wheels, ruff clean.

15. **Task - The last criterion named a syntax copier lacks** (v0.6.10): Resolved the one open acceptance criterion by checking its premise against copier rather than against `copier_ui`<br>
    **Result**: `Choice normalisation` had stood open since 0.1.0 on the note that copier 9.17.2 refuses the list-of-single-key-dicts form. Re-checked the premise directly: `copier.run_copy` on a template declaring `choices:\n - Small: s` raises `ValueError: Could not convert {'Small': 's'} to string` with no `copier_ui` anywhere in the call, so the criterion had been asking for a third form copier does not have - unachievable as written, not unbuilt. Reworded to copier's own three shapes - bare list, label-to-value dict, list of `[label, value]` pairs - all three already normalised and tested in `tests/unit/test_ui_schema.py`, and closed. Added an edge criterion for the refusal itself: a `choices` shape copier cannot read leaves the field disabled carrying copier's own message, never a syntax `copier_ui` invented, which is what the adapter's per-field error path already does. `docs/acc-crit-copier-tui.md` now reports 93 of 93 closed, `acc-crit.py check` clean at 0 errors 0 warnings. 170 unit tests, ruff clean.

16. **Task - Release 0.6.11 to PyPI** (v0.6.11): Published both packages, the first release carrying the rebuilt survey<br>
    **Result**: PyPI had been sitting at 0.6.8 while the tree moved to 0.6.10 - 0.6.9 and 0.6.10 were bumps `make install` makes on every run, never uploads - so this release carries five entries of work at once. Shipped in `copier_ui`: `Schema.groups` with the `_ui_groups` opt-in and the single-untitled-group fallback, `Question.condition_ids` naming what a `when` reads, and the validation surface. Shipped in `copier_tui`: the bare-accordion survey - `inline.py` printing every option on the row, left-aligned wrapping captions in a 56-column gutter, `WrapInput` for answers that outgrow one line, per-row help, and a review screen that wraps rather than truncates. `docs/research-settings-ui.md` ships as the evidence base those choices were judged against. 93 of 93 acceptance criteria closed.

17. **Task [Extended] - Legible rows, honest affordances, and the keyboard back** (v1.0.2): Fixed the dead "press any key to close" and rebuilt the form's visual encoding around ground rather than hue<br>
    **Result**: Four reports from a real run against `gh:galaxa-labs-ai/copier-galaxahub-deployment`. The exit failure is not in the app: `ExecutionScreen.on_key` fires and the pilot has always closed the screen on any key. It is copier's tasks - proven by running `run_copy` on a probe template whose task prints, and watching `TASK-WROTE-TO-STDOUT` arrive on the parent's own stdout. Tasks are ordinary subprocesses holding this process's descriptors, so under `--trust` a task inherits the terminal Textual is holding in raw mode: whatever it reads is a keystroke meant for the form, and a task that prompts leaves the tty in its own mode. `_detached_stdin` in `execution.py` puts descriptor 0 on `/dev/null` for the render and restores it after; 1 and 2 are deliberately left alone, since Textual paints the screen through 1 and blanking it would take the progress bar and the verdict with the task's output. The second report - lines that "appear blue as if non mutable, but we switch to it anyway" - was `_label_text` colouring a caption `bold CYAN_BRIGHT` whenever its answer had moved off the default. That encoded a property of the answer as a property of the question, and blue reads as heading or read-only. Hue now means focus alone: the stylesheet colours captions and a genuinely inert row gets a `row-off` class greying caption and control together. The remaining three - indistinguishable lines, inputs that do not look like inputs, selections with no selected state - are one scheme. Rows alternate `#1a1f25` and `#252b32` from the skill's own ladder, restriped by position on every sync so a conditional question appearing flips everything below it rather than leaving two same-coloured rows adjacent. Typing surfaces carry `#303841`, lifted under focus and banded on alternate rows, because a full-width control otherwise paints over the stripe and five consecutive text questions merged into one lit block. Option rows deliberately carry no typing ground - the chip does that work: the answer sits on filled cyan with dark text, the cursor on chrome, the passed-over plain, so taken and dismissed differ by ground rather than by which blue is brighter. New `tests/unit/test_tui_appearance.py` holds seven tests including the restripe and the descriptor round trip; 177 unit, 8 functional, six criteria closed at 99 of 99. Version set to 1.0.2 by request, README gains an install quick start and the scheme's description.

18. **Task - Release 1.0.3 to PyPI** (v1.0.3): Published the first 1.x, carrying the exit fix and the form's new visual encoding<br>
    **Result**: PyPI went 0.6.11 to 1.0.3 - the manifests were set to 1.0.2 by hand and `make publish` took its own patch on top, which is the Makefile's standing behaviour and was left to do its job. Shipped: `_detached_stdin` round the copier run so a template task under `--trust` can no longer inherit the terminal and kill "press any key to close"; captions coloured by focus rather than by whether their answer moved off the default; alternating row bands restriped by position; typing grounds that carry the band so consecutive text questions stop merging into one block; filled chips marking the option in force against the ones passed over; `row-off` greying a disabled question's caption with its control. README gained an install quick start. 177 unit tests, 8 functional, 99 of 99 criteria closed.

19. **Task [Extended] - Options as chips, and two silent bugs behind them** (v1.0.4): Rebuilt the option states after a real run showed captions reading as answers, and found a dead focus stop and an inert stylesheet on the way<br>
    **Result**: Four reports, two of which turned out to be structural. Only the chosen option had a ground, so an unchosen label was bare text - and bare text beside chips reads as another option, which is why a caption beginning "How is this deployment..." looked like one of its own answers. Every option now carries a ground in one of three states: chosen on filled cyan, cursor on the brightest neutral, the rest a step back at 5.45:1, all three clearing AA because the options passed over are the alternatives being decided against. Only the answer changes hue, so chosen is never inferred from which neutral is brighter, and a ticked multiselect option reads like a chosen single one. The bands were invisible not because a 256-colour terminal collapsed them - checked, they map to 16 and 235 - but because an 11-per-channel step cannot be seen; widened to 16-21. "Inert lines selectable" was real and was not the disabled rows, which Textual already refuses to focus: the `VerticalScroll` takes focus by default, so the walk ran form, control, form, control, and every other press landed where nothing was highlighted or editable. It is `can_focus = False` now. Removing it exposed that focus-on-mount had been riding on that container: a row mounts a beat before the control it composes, so `_focus_field` found nothing and swallowed it, and the form opened with no focused row. `_refresh_rows` is awaited now. Third find, from a test that had been passing for the wrong reason: Textual ranks app CSS above a widget's default sheet whatever the specificity, so every typing-ground rule in `FieldRow.DEFAULT_CSS` was dead and the generic `Input` rule won - invisible while `FIELD_BG` happened to equal it. Moved to `BASE_CSS`. Focused rows now keep a blank line either side. 180 unit, 8 functional, 105 of 105 criteria closed.

20. **Task - Release 1.0.4 to PyPI** (v1.0.4): Published the option-chip rebuild and the two focus bugs it uncovered<br>
    **Result**: Second release of the day, carrying what entry 19 describes. Shipped: every option a chip in one of three grounds - chosen on filled cyan, cursor on the brightest neutral, passed-over a step back, all clearing 4.5:1 - so an unchosen label is never bare text a caption can be mistaken for; row bands widened from an 11-per-channel step that could not be seen to 16-21; `VerticalScroll.can_focus = False` removing the dead stop between every pair of questions; awaited row mounts so the form opens with its first field focused rather than nothing; typing-ground rules moved into `BASE_CSS`, Textual ranking app CSS above a widget's default sheet whatever the specificity; a blank line either side of the focused row. 180 unit tests, 8 functional, 105 of 105 criteria closed.

21. **Task [Extended] - Circles, stacking, and light ink on the answer** (v1.0.5): Reworked the option row after a real run - shape as well as colour, a cursor that survives resting on the answer, and layout that adapts to label length<br>
    **Result**: Four changes to `inline.py`. Every option now carries a filled circle when it is the answer and an empty one when it is not, so chosen no longer rests on colour alone; both glyphs measure one cell, checked, so the label column does not shift. The chosen chip moved from brand cyan to `#0b6591` because the request was light text on it and white on `#21a8e4` measures 2.70:1 - dark ink on a bright chip had been reading as struck out rather than chosen; white on the deeper blue is 6.39:1. The cursor got its own mark rather than a ground, because the cursor starts on the answer every time and a ground that already says "chosen" has nothing left to say "and you are here". Painting it exposed that `has_focus` is a reactive Textual has not finished setting when it delivers the Focus event, so the row drew as blurred on arrival and the mark never appeared; focus is tracked on the widget now. Options share a line while they fit the row's measured width and stack one per line once they do not, repainting on resize. 183 unit tests, 109 of 109 criteria closed.

22. **Task - A cursor that can actually be seen** (v1.0.5): Replaced the underline marking the cursor's option after it proved close to invisible in use<br>
    **Result**: Three treatments were measured before one held. Inverting the chip under the cursor was tried and dropped: it cost the answer its blue ground and white ink, which is the pairing that reads as chosen. A ground bright enough to stand alone cannot carry white text - the blues clearing 4.5:1 with white stop at `#1477b4`, a modest step from the resting `#0b6591`. A coloured mark inside the chip has only the chip to contrast against, where amber measures 2.91:1. The mark therefore sits outside the chip, on the row's own dark band, where a bright cyan caret has the whole band to read against; the chip under it steps to `#1477b4` and goes bold. Chosen still reads off the shape, filled against empty, so the answer keeps blue and white throughout. 183 unit tests, 110 of 110 criteria closed.

23. **Task - Release 1.0.5 to PyPI** (v1.0.5): Published the option-row rework described in entries 21 and 22<br>
    **Result**: Shipped: a filled circle on the answer and an empty one on every alternative, so chosen is said in the shape as well as the ground; the chosen chip moved to `#0b6591` so it can carry white text, the brand cyan managing only 2.70:1 against white and reading as struck out with dark ink; options sharing a line while they fit the row's measured width and stacking one per line once they do not, repainted on resize; and a cursor carried by a bright caret on the row band, a brighter chip and bold text together, after an underline proved close to invisible and both an inverted chip and an in-chip mark were measured and rejected. 183 unit tests, 8 functional, 110 of 110 criteria closed.

24. **Task [Extended] - The questionnaire names itself, and the answers get their own ink** (v1.0.6): Five requested changes plus a breathing focus bar<br>
    **Result**: The header now reads `<template> questionnaire - N of M` instead of `survey`. Which template is being filled in is the one fact none of its own questions carries, and it belongs to `copier_ui` rather than the renderer, so `TemplateAdapter.source_name` reads it back off the worker - the command-line argument is absent on an update, where copier takes the source from the answers file - and drops a `.git` suffix that names the transport rather than the template. Answers moved off the reading grey onto a pale yellow, warming to `#ffc857` in the cell being edited, at 7.16:1 and 5.06:1 on their grounds; on a form this long the questions and the answers to them are the two things most worth telling apart. `ctrl+c` is no longer bound: it used to cancel, so reaching for copy threw away every answer given, and Textual already hands it to a focused input as copy of its own. A space now sits between the cursor caret and the option's circle. The focus bar breathes through four shades over about two seconds, driven by classes rather than inline styles because Textual has no way to clear an inline style once set - the bar would keep its last shade after the focus moved on. 186 unit tests, 114 of 114 criteria closed.

25. **Task - Release 1.0.6 to PyPI** (v1.0.6): Published the changes described in entry 24<br>
    **Result**: Shipped: the header naming the template and calling the screen a questionnaire, read back off the worker in `copier_ui` so an update - where copier takes the source from the answers file rather than the command line - still finds it; answers in pale yellow warming to `#ffc857` under the cursor, at 7.16:1 and 5.06:1 on their grounds; `ctrl+c` unbound, since it used to cancel and reaching for copy threw away every answer given; a space between the cursor caret and an option's circle; and the focus bar breathing through four shades over about two seconds, driven by classes because Textual cannot clear an inline style once set. 186 unit tests, 8 functional, 114 of 114 criteria closed.

26. **Task - Answer ink, smooth breath, focused row ground** (v1.0.7): Four appearance corrections after the Star Colonel used 1.0.6<br>
    **Result**: Pale yellow on settled answers read as a warning about all of them, so `ANSWER_FG` went neutral and brighter than the captions (`#dcdcdc`, 7.43:1), and `ANSWER_FOCUS_FG` moved from amber to orange (`#ffb866`, hue 40 -> 32). The orange is capped at 4.56:1 by `FIELD_FOCUS_BG` being the lightest ground on the form - hue 30 at full saturation measures 3.65:1 there, so going further orange means darkening that ground, which is the cue saying which cell is live. The focus bar's four shades a third of a second apart read as a sequence, not a breath: `PULSE_SHADES` is now 32 cosine-eased phases at 0.06s, no frame moving a channel more than 10 of 255, easing through the turnarounds. `ROW_FOCUS_BG` / `ROW_ALT_FOCUS_BG` lean each band eight parts of blue under the cursor. Demo author in `screenshots.py` is now AcmeCo; all three `docs/assets` screens recaptured. 188 tests, 115 of 115 criteria.

27. **Task - Medium article and its generated cover** (v1.0.7): A short launch article for the project, written to the author's stored voice profile<br>
    **Result**: `docs/medium/copier-tui.md`, 527 words, built from the `stellars` profile in the article-writer plugin's own database, which locks the mechanics: no claim without a number, name a concept then define it in one line, fence the scope before the reader asks. Every figure re-measured rather than recalled: 35 questions and 12 conditional counted out of the reference template, nine question kinds off the `Kind` enum, 60x18 from the minimum-size constants. The hero image needed a fallback - `amazon.nova-canvas-v1:0` is refused account-wide as Legacy after 30 days unused, and both Titan image generators are end-of-life, so `stability.stable-image-core-v1:1` in us-west-2 produced it, three prompt versions logged in `images/article-cover.txt`. A negative prompt reading `orange dominant` removed orange from the render entirely rather than demoting it. The three cover themes come from that one render by unpremultiplying `alpha = max(R,G,B)/255` at a 0.75 gamma lift, reconstructing over black to within 2 of 255.

28. **Task - README cut to house style** (v1.0.7): Trimmed 246 lines to 131 and dropped two of the three screenshots<br>
    **Result**: The README carried roughly 2000 words of prose against 98 in `streamlit_chat_input_fileupload` and 71 in `uv-bash-autocompletion`, the two comparable published-package repos - their shape is badges, two sentences, one screenshot, then terse `## Features` bullets. The `## Screens` section was the bulk of it: about 1000 words describing colour, cursor cues, banding and caption wrapping, all of which is already carried by `theme.py`, the acceptance criteria and this journal, so it became seven bullets. `Why`, `Screens`, `Usage` and `Project Organization` collapsed into one `## Architecture` keeping the layer diagram, the no-terminal rule, the widget table and a six-line snippet; the repo tree went, since GitHub shows it. Flag semantics became bullets. Added the pepy downloads badge for `copier-tui`. `docs/assets/review.svg` and `execution.svg` are now referenced by nothing while `make screenshots` still captures them.

29. **Task - Conditional rows say so twice** (v1.0.7): A question another answer gates now leans its ground green and indents its caption<br>
    **Result**: Conditional questions were indistinguishable from any other row, though `Question.condition_ids` - the ids a `when` reads - had been carried by `copier_ui` since the schema was built and no renderer read it. `SurveyScreen._refresh_rows` now sets `row-cond` from it, so `copier_tui` still never parses a `when`. `ROW_COND_BG` / `ROW_ALT_COND_BG` raise green 8 and drop blue 6 on each band: a hue lean rather than a brightness one, under half a percent of luminance, captions at 8.80:1 and 6.87:1. Green because the cursor already owns the blue lean and two tints of one hue read as neither; the focus tint wins on a focused row, the two-column caption indent carrying the meaning there. The gutter keeps its width, so the control column never moves. 190 tests, 116 criteria, `docs/assets/survey.svg` recaptured.

30. **Task - Focus plate and the conditional typing ground** (v1.0.7): The tint on both cues now covers the whole row rather than part of it<br>
    **Result**: The blank line above and below the focused question was `margin`, which sits outside the widget and therefore took the form's ground - the plate was a one-line strip with the screen showing through either side. It is `padding: 1 0` now, so the row paints all three lines and the breathing bar runs the height of the plate. The conditional lean stopped at the caption for the same reason in reverse: a control spans the row, so `FIELD_COND_BG` `#394a47` and `FIELD_ALT_COND_BG` `#3f504e` carry the same green 8 / blue 6 lean into the typing ground, at 6.82:1 and 6.20:1 for the answer ink. A row wearing both `row-alt` and `row-cond` matches on one class more than the focus rule and outranked it, so the focused case is spelled out for the row and its control. 192 tests, `docs/assets/survey.svg` recaptured.

31. **Task - Conditional questions drawn as a tree** (v1.0.7): A connector before the caption hangs each conditional row off the answer that decides it<br>
    **Result**: The green tint and the two-column caption indent said a row was conditional but not what it hung from. The indent is now the connector itself: `BRANCH_MORE` and `BRANCH_LAST` in `survey.py`, printed by `FieldRow.set_branch` in muted ink so structure never brightens with the caption under focus. `_branch_glyph` gives all but the last child of one answer the through connector, comparing `condition_ids` with the next visible row - four children of `dataset_storage` each closing their own run would read as four runs of one. A connector is suppressed when no governing row is on the form, which is what an answer supplied with `--data` leaves behind, since it would otherwise point at the unrelated question above. New fixture `tests/fixtures/tui_tree`, two children under one choice. 194 tests, 118 criteria.

32. **Task - Release 1.0.8 to PyPI** (v1.0.8): Published the conditional-question cues described in entries 29 to 31<br>
    **Result**: Shipped: a green ground on every question another answer decides whether to ask - `ROW_COND_BG` `#1a271f` and `ROW_ALT_COND_BG` `#2a3934` per band, the same green 8 / blue 6 lean carried into the typing ground at `#394a47` and `#3f504e`, since a control spans the row and a neutral ground there cut the tint in half; and a tree connector before the caption, through for every child of one answer but the last. The blank lines around the focused row became `padding` rather than `margin`, so the plate under the cursor is one object instead of a strip with the form showing through. A row wearing both `row-alt` and `row-cond` outranked the focus rule on specificity and now yields to it explicitly. 194 tests, 118 of 118 criteria.

33. **Task - Wrapped conditional captions hang off their connector** (v1.0.9): The second line of a long caption ran back under the tree glyph<br>
    **Result**: A caption longer than the 53-column gutter folded to column 0, putting prose in the connector column and breaking the line the connectors are read down - visible on any template whose questions carry real `help` text rather than bare variable names. `_label_text` now wraps the caption itself with `textwrap.wrap` instead of leaving it to Rich, which has no hanging indent: the first line takes the connector, and every line after it takes `│  ` on a child with siblings below it and three spaces on the last. `BRANCH_MORE` and `BRANCH_LAST` moved from `survey.py` to `widgets.py`, since how a caption continues is the row's own business, and `survey.py` imports them. `LABEL_LINES` still caps a caption at two lines. 195 tests.

34. **Task - The run crosses the cursor's gap, and the mark breathes** (v1.0.10): Two cues that stopped at the edge of the focused row<br>
    **Result**: The blank line the focused row keeps either side of itself was `padding`, which takes the row's ground but is empty by definition, so landing the cursor inside a run of children cut the connector column in half. The spacing is now two `field-rail` Statics of the row's own, carrying `│` where the run actually crosses them and nothing above a first child or below a last - `_branch` returns the connector and whether a sibling sits above, and `set_branch` fills both. `size.height` counts a spacer where it did not count padding, so `test_a_plain_field_costs_exactly_one_row` now measures the caption line rather than the row. The option cursor mark takes its shade from `FieldRow._breathe` rather than a timer of its own, so mark and bar cannot drift apart. 197 tests, 121 criteria. The `text-user-interface` skill gained the long-form, tree and spacing sections.

35. **Task - The screens name the destination and the files** (v1.0.10): Two facts the run was keeping to itself<br>
    **Result**: The survey's bottom line now carries `→ <dst>` beside the key legend - `_where_text` in `survey.py` shortens a path under `$HOME` to `~/`, and `SurveyScreen` takes `dst` for it. The header already held the template name and the field position, and where the render lands is the one fact a person thirty answers deep cannot recover from anything else on the screen. The execution screen gained a `RichLog` (`#exec-files`) fed by `_show_new_files`, re-walking the destination every `WATCH_INTERVAL` 0.2s, skipping `.git` and stopping at `WATCH_LIMIT` 2000 entries so a template writing a `node_modules` cannot stall the screen - copier reports nothing while it works, so the destination is the run's only account of itself. A dry run writes `nothing written`.

36. **Task - A render can no longer deafen the form** (v1.0.10): A template's own tasks took the keyboard, and 'press any key to close' answered nothing<br>
    **Result**: Two routes from a task to the terminal, both now shut. `_children_without_stdin` patches `subprocess.Popen.__init__` for the render window so everything copier starts gets `DEVNULL`, replacing the first cure - which pointed THIS process's descriptor 0 at `/dev/null` and spun Textual's input thread 476k times on end-of-file, leaving the app deaf. `_terminal_mode_kept` snapshots `termios.tcgetattr` before the render and restores it after: descriptors 1 and 2 still reach the terminal, `stty sane </dev/tty` is one line of an ordinary hook, and a cooked line discipline holds every keystroke until a newline. New `tests/unit/test_tui_terminal.py` drives the whole app through `pty.fork` against fixture `tui_rude`, whose tasks do both; without the guard the keypress echoes and the process never exits. 201 tests, 124 criteria.

37. **Task - Release 1.0.11 to PyPI** (v1.0.11): Published the terminal shield and the two facts the screens were withholding, entries 35 and 36<br>
    **Result**: Shipped: a render that cannot deafen the form. Everything copier starts gets `DEVNULL` for stdin through a patched `subprocess.Popen.__init__`, and the terminal's line discipline is snapshotted and restored around the render, closing the second route - descriptors 1 and 2 still reach the terminal, so `stty sane </dev/tty` in a task cooked it and every keystroke then waited for a newline that "press any key to close" never asks for. Also shipped: `→ <dst>` on the survey's status line, and a `RichLog` on the execution screen listing files as copier writes them. `tests/unit/test_tui_terminal.py` drives the whole app through `pty.fork` against the new `tui_rude` fixture; with the guard removed the test hangs and the keypress echoes. README gained one feature bullet. 201 tests, 124 criteria.

38. **Task - Option rows stop shrinking into place** (v1.0.11): Questions with several options opened four lines tall and closed up only when the cursor reached them<br>
    **Result**: Reproduced in a real pty at the reporter's own 164x55 over `copier-data-science`: five-option questions left one blank line per option behind them. `InlineOptions._paint` chose one line versus a stack from `self.size.width or INLINE_BUDGET`, and before the first layout there is no width, so the 42-column guess stacked every long question: the row was laid out at the stack's height, the real width arrived, it repainted on one line, and the vacated lines stayed on screen because only its own region was dirty. Anything redrawing that patch cleared them, which is why the cursor seemed to. `_fits` now answers one line when it has no width, so a row grows into the stack rather than shrinking out of it; `INLINE_BUDGET` deleted. A new test records every paint and allows none to stack where the options fit, failing `[5, 5, 1, 1, 1]` on the old rule. 203 tests, 125 criteria.

39. **Task - Release 1.0.13 to PyPI** (v1.0.13): Published the option-row fix from entry 38, and put the release on the machine's PATH<br>
    **Result**: Shipped: a question with several options no longer opens tall and closes up when the cursor reaches it. `_fits` in `inline.py` answers one line while the row has no width to measure, so a row grows into its stack instead of shrinking out of it and leaving vacated lines on the screen. The reporter had been running 1.0.10 from `/opt/conda/bin`, two releases behind the source, which is why they saw the full display of it - so `/release-project` gained an Install step: after publishing, upgrade whatever environment the console script resolves from and confirm `--version` matches, a stale command on PATH counting as an error rather than a warning. 1.0.12 was burnt by an interrupted publish that bumped before it was stopped, and never reached PyPI. 203 tests, 125 criteria.

40. **Task - The survey gets its whole screen back** (v1.0.13): Four defects reported against 1.0.13 - a body cut short under a full-width footer, a scrollbar over half-filled content, an invisible destination, and a double-escape that would not quit<br>
    **Result**: One cause under three of them. `DEFAULT_CSS` styles `#survey-status` at `height: 1`, but the `Horizontal` holding the legend and the destination carried no id, so it fell back to `Horizontal`'s own `1fr` and split the screen with the form: at 164x55 the form got 26 rows of 51, overflowed, scrolled, and the rows the status row hoarded stayed blank to the footer. Giving the row its id restores all three. The destination wrapped onto three lines because Textual's visual pipeline drops a Rich `Text`'s `no_wrap` and `overflow`; the crop moved to CSS. Double-escape failed because Textual asks for every pointer movement (`?1003h`) and a bare escape stays ambiguous until the next byte: a mouse report behind the second escape becomes its introducer and eats it, reproduced as `1b1b5b49`. `run(mouse=False)` removes the reports; the survey is keyboard-driven anyway. `_step_focus` replaces Textual's rolling `focus_next`. 219 tests, 126 criteria, tracked in new `docs/defects-copier-tui.md`.

41. **Task [Extended] - Exit paths, path display, cell-aware layout, and an 11-round review** (v1.0.15): Star Colonel reported the app nonresponsive on its final screen, `.` shown as the destination, and the cursor row indistinguishable from a selected option<br>
    **Result**: Root cause of the hang: `enter` and `escape` on the "press any key" screen were bound to an action, and `Screen.dismiss()` from inside an action dispatch waits on that dispatch - a deadlock in every release; every other key went through `on_key` and worked, and the one close test sent a bare `x`. Bindings removed, all keys take one route, pty test sends `enter`/`escape` and asserts exit. The `.` path: `update` defaults its destination to the cwd; new `paths.py` resolves it home-relative with `~` and crops long paths from the LEFT with `.../`, measured in cells not characters, and survives a missing home. The binary on PATH was 1.0.11, three releases stale, which is why fixes never reached the terminal. Cursor marker is now `◼` U+25FC (East Asian width N - a runtime audit found seven ambiguous-width glyphs the source scan missed) with inverted ink on `CURSOR_BG`, distinct from `CURSOR_PICKED_BG`. Option labels wrap cell-aware via `divide_line`, painted and counted by one function pair. `update --trust` on a dirty or non-git destination refuses before fetching, copier's own wording. Survey gate: a row's error sentence waits until it is edited or enter is refused, and the status line counts refused answers down. Adversarial review ran eleven rounds - rounds 3-9 each seeded a regression from the previous fix (the resize advisory alone took three rounds, resolved by deleting the overlay), which violated the two-clean-rounds stop rule and cost ~7 hours; round 10 adjudicated four ux majors into six minimal changes, round 11 converged on one item (failure reason in a `wrap=False` log; then Textual's `RichLog` default `min_width=78` folding past the box at 60 columns), closed with `wrap=True, min_width=0` and verified with the reviewer's own repro. Defect log: 178 items, 7 added this round, DEF-RVW-53 (header tail-crops names over 23 chars at 60-63 columns) and DEF-SRVY-59 deferred with reasons, DEF-SRVY-52 open. 358 unit tests green, functional suite green against built wheels. Not yet published or committed - 24 files, to land as four commits.

42. **Task [Short] - Release 1.0.16** (v1.0.16): Published copier-ui and copier-tui 1.0.16 to PyPI<br>
    **Result**: `make publish` bumped 1.0.14 → 1.0.16 (1.0.15 consumed by the functional-test build, never published), 358 unit tests green inside the build, both wheels live on PyPI, `/opt/conda/bin/copier-tui` upgraded to 1.0.16 and a live pty run against `copier-data-science` wrote 74 files and closed on enter with exit 0.

43. **Task [Extended] - Finish banner, project in the header, destination word, white cursor breath** (v1.0.16): Star Colonel asked for a success popup with change counts, the project name in the header, `destination:` instead of an arrow, and a cursor square pulsing white to cyan<br>
    **Result**: New `changes.py` snapshots the destination (path → blake2b digest, `.git` pruned) before the render and after, and reports added / changed / deleted / conflicted - a conflict being a `.rej` beside a file or an inline `<<<<<<<` marker, since copier writes either depending on `--conflict`. The execution screen shows a content-sized `Static` on `layer: overlay` per the TUI skill's popup pattern: `done` and the four counts with conflicted paths listed, or `failed` with copier's reason; any key closes. `HeaderBar` takes the destination's resolved name (`paths.project_name`) on all three screens, left-cropped behind `...` via `fit_name` when too wide - which made the review header's path redundant, so it went and DEF-RVW-53 closed as superseded. The destination line reads `destination: <path>`, takes the rest of the row after an `auto`-width hint, and re-fits from its own `on_resize` because a hint appearing shrinks it without a screen resize. `MARK_SHADES` ramps white to `CYAN_BRIGHT` in 17 steps on the bar's beat. The counts line uses U+2E31, not U+00B7 - the glyph-width test caught the middle dot. 363 unit tests green; live pty run showed `30 added ⸱ 0 changed ⸱ 0 deleted ⸱ 0 conflicts` and closed on enter.
